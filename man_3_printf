.TH man 3 "July 11 2023" "Puzzle"

.SH NAME

_printf - A custom implementation of the printf() function

.SH SYNOPSIS

.B #include "main.h"

.B int _printf(const char *format, ...);

.SH DESCRIPTION

.B _printf()
is a custom implementation of the
.BR printf()
function. It can be used to print formatted output to the standard output stream.

The syntax of the
.B _printf()
function is as follows:

.BI _printf("format", arguments);

The
.I format
string is a format string that specifies how the output should be formatted. The
.I arguments
are the values that will be used to fill in the format specifiers in the
.I format
string.

.SH FORMAT SPECIFIERS

.B _printf()
supports the following format specifiers:

.TP
.B c
Print a single character.
.TP
.B s
Print a string of characters.
.TP
.B d
Print a decimal integer.
.TP
.B i
Print an integer.
.TP
.B %
Print a literal percent sign.

.SH EXAMPLES

.RS
.B _printf("This is a test.\n");
.br
.B _printf("The value of %d is 10.\n", 10);
.br
.B _printf("The string \"hello\" is printed.\n");
.RE

.SH SEE ALSO

.BR printf(3)

.SH AUTHOR

This man page was written by Michael/Puzzle Moser

.SH BUGS

No known bugs.

